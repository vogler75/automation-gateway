rootProject.name = 'gateway'

include('app', 'test')
include('cluster-template', 'cluster-gateway', 'cluster-opcua', 'cluster-plc4x', 'cluster-influxdb', 'cluster-cache')
include('lib-core', 'lib-influxdb')

if (System.env.DDS_ROOT) {
    include ('cluster-dds')
} else {
    print "Environment variable DDS_ROOT is not set."
}

