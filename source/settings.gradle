rootProject.name = 'gateway'

include('app')
include('cluster-template', 'cluster-gateway', 'cluster-influxdb', 'cluster-plc4x')
include('lib-core', 'lib-cluster', 'lib-influxdb')

if (System.env.DDS_ROOT) {
    include ('cluster-dds')
} else {
    print "Environment variable DDS_ROOT is not set."
}

